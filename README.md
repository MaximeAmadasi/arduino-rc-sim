# Arduino RC simulator
Use an Arduino board to transform your RC receiver to a game controller

Actually, only Spektrum Sattelite are compatibles. You need to use an Arduino Leonardo or an Arduino mini
